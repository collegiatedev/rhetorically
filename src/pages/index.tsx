import Head from "next/head";
import Link from "next/link";
import { api } from "~/utils/api";
import {
  SignInButton,
  SignOutButton,
  UserButton,
  useUser,
} from "@clerk/nextjs";

import {
  Table,
  TableBody,
  TableCaption,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from "~/components/ui/table";
import { Button, buttonVariants } from "~/components/ui/button";
import { Input } from "~/components/ui/input";

export default function Home() {
  const { data } = api.essays.getAll.useQuery();
  const user = useUser();

  return (
    <>
      <Head>
        <title>Collegiate</title>
        <meta
          name="AI College Admission Tools"
          content="Generated by Team Collegiate"
        />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <nav className="bg-gray-800 p-4">
        <div className="container mx-auto flex items-center justify-between">
          <a href="https://www.collegiate-consulting.com/">
            <img
              src="/horizontal-white.svg"
              alt="Brand Logo"
              className="h-8 w-auto"
            />
          </a>
          <div className="flex flex-grow items-center justify-center space-x-4 text-white">
            <a href="/home" className="hover:text-gray-300">
              Home
            </a>
            <a href="/about" className="hover:text-gray-300">
              About
            </a>
            <a href="/contact" className="hover:text-gray-300">
              Contact
            </a>
          </div>
          <div>
            {!user.isSignedIn && (
              <Button variant="primary">
                <SignInButton />
              </Button>
            )}
            {!!user.isSignedIn && (
              <Button variant="outline">
                <SignOutButton />
              </Button>
            )}
          </div>
        </div>
      </nav>

      <main>
        <div className="mt-8 flex justify-center">
          <div className="w-full max-w-xs">
            <Input
              type="number"
              placeholder="GPA: 4.3"
              className="mb-1"
              step="0.1"
              value="3.5"
            />
            <p className="mb-4 text-sm text-gray-600">Enter your GPA</p>
            <Input
              type="number"
              placeholder="College List Length: 12"
              className="mb-1"
              value="12"
            />
            <p className="mb-4 text-sm text-gray-600">
              Enter the amount of colleges you would like to add to your college
              list
            </p>
          </div>
        </div>

        <div>
          {data?.map((essay) => (
            <div key={essay.id}>{essay.content}</div>
          ))}
        </div>
      </main>
    </>
  );
}
